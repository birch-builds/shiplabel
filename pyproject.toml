[project]
name = "shiplabel"
version = "0.1.0"
description = "Validating parser and pretty printer for a plain-text shipping label format"
requires-python = ">=3.9"
readme = "README.md"
license = { text = "MIT" }
dependencies = []

[tool.setuptools.packages.find]
include = ["shiplabel*"]
